import React from 'react';
import classes from './SideTray.module.css';

const SideTray = (props) => (
    <header className={classes.SideTray}>
        <p>Details about the PDF elements</p>
    </header>
);

export default SideTray;